## 1. Gate first (RED, snapshot-pinned)

- [x] 1.1 Snapshot fixture: copy the 240 input rows (id, text, start/end ms, token_timestamps JSON) from the live DB into `tests/fixtures/cde5c264_transcripts.json` with the FULL meeting id and a SHA-256 over the ordered rows (pinned 2026-09-08, sha256 `ad1ebbb777110d59d28c86f8ef009d3aba3dd801c2b92e637cddd94a98f37e1c`, via `tools/snapshot_fixture.py`)
- [x] 1.2 Harden the gate's replay input: replay from the snapshot; live-DB cross-check uses exact-equality meeting lookup, asserts exactly one meeting match, asserts row count + hash, and hard-fails "fixture drifted — re-pin" on mismatch (never vacuous-green on 0 rows) — DONE (implemented in `ear_truth_gate.rs`; exercised on every gate run through 2026-09-09, current pin 182 rows / sha af1e2465…)
- [x] 1.3 Fracture assertion: row i>0 fails iff `is_mid_sentence_start(text_i)` AND badge differs from previous persisted row AND that previous row lacks terminal punctuation (closing quotes/brackets trimmed); row 0 exempt — DONE, AMENDED 2026-09-09 by the user ear decree: the fracture scan routes through NO waiver path (every fracture fails hard); the amendment-record waiver path remains for the other render-level residuals only
- [x] 1.4 Duplicate-cluster assertion: ≥3-token contiguous shared subsequence covering ≥80% of the shorter row, disjoint spans, gap ≤2 s, different badges or one side Unknown; scan window ±10 s (not strict adjacency); add the `overlapping-span rows` counter (same-audio double-decode suspects: reported + render_failures entry, never dropped); annotate the tautological turn-level invariant scan so its "0 violations" is not cited as evidence — DONE (implemented in `ear_truth_gate.rs`; the gate output labels the invariant scan "TAUTOLOGICAL — not evidence")
- [x] 1.5 Confirm RED on the snapshot with the shipped predicates: measure and pin the true fracture count and the [74.28]/[80.34]/[85.49] cluster (NOT the 163 raw-lowercase number); print every offender — DONE 2026-09-08 (`openspec/exploration/no-split-1.5-red-20260908.log`): snapshot replay pinned (240 rows, live DB cross-check matched sha ad1ebbb7…); old pipeline reproduced 240→284→243; TRUE fracture count = **155 cross-badge fractures** of 243 persisted rows (raw-lowercase screen: 163); duplicate scan = 1 cluster ([74.28] sp1 + [80.34] Unknown, 10/11 shared tokens; the [85.49] third member had already been absorbed by consolidation); overlapping-span rows 0; ear entries 15 PASS / 1 known-limitation / 0 FAILED; churn 2.9 rows/min, 11 rows of ≤2 words. Gate exit 101 (RED) on the two new assertions.

## 2. Sentence-atom alignment (GREEN)

- [x] 2.1 Fragment rejoin + pure segmentation: rejoin adjacent persisted rows whose predecessor lacks sentence-terminal punctuation into logical units (unit tests: "I" + "don't know. Let me ping in..." rejoins; "Oka|y." glues); terminators `.?!` plus full-width `。？！` with `…`/`...` normalized pre-split; drop non-word atoms; whole-atom majority assignment for short straddling sentences; bounded-run guard cuts an atom spanning >1 turn AND >8 s at the turn boundary with the tail flagged `cross_badge_tail`; unit tests on real strings ("Where is Ricardo? I don't", "I", "don't know. Let me ping in.", "that's not going to... Like, motors is a", "Oka|y.")
- [x] 2.2 Span sources: valid-token path only when token JSON passes the ≤25 tokens/second sanity clamp AND token-word count matches the row's whitespace tokenization (segment the token-word list); otherwise proportional per-sentence shares of the unit's span (the 237/240 default); clamp unit test with a hallucinated-blob fixture (the measured 11 kB/1 s rows must fall back to proportional)
- [x] 2.3 Sentence→turn assignment: maximize overlap_ms; tiebreak = edge distance from sentence midpoint (containment-0); near-tie within 100 ms prefers the previous atom's badge; align emits Unknown ONLY for sentences overlapping no turn — `assign_engine_gap_fragments` stays the sole cap-borrow (no second fallback); determinism unit test on the tie case
- [x] 2.4 Wire into `align_transcripts_with_diarization` (atoms flow through merge → consolidate unchanged) and rewrite the existing split-pinning tests to sentence-atom expectations: `token_alignment_splits_multi_speaker`, the A→B→A three-way split test, `proportional_cjk_no_whitespace_is_divided` (single-sentence no-whitespace row now persists whole under one badge), and the remaining alignment split tests
- [x] 2.5 Duplicate resolution at persist: keep the badge-agreeing labeled copy, write text once, extend the survivor's span to the cluster union, delete absorbed shells AND their source rows in the transaction (guard against the `persist_aligned_splits` empty-group orphan); unit tests: the measured 3-row cluster resolves with coverage preserved; "I can't. I can't." and a "Yeah," interjection are NOT duplicates; content-preservation invariant holds end-to-end
- [x] 2.6 `stamp_continuation_facts` first-row match: select the first row OVERLAPPING the turn span (start < t.end AND end > t.start) instead of `audio_start_time >= t.start`, so majority-assigned sentences starting before the turn still receive the stamp

## 3. Verification

- [x] 3.1 Snapshot gate GREEN — DONE 2026-09-09 (`no-split-3.1-green-wholeatom-20260909.log`, gate exit 0). Two states to record: (a) 2026-09-08 (`no-split-3.1-green-20260908.log`) had 0 UNEXPECTED fractures but 56 bounded-run cross-badge tails under the (then-designed) waiver-gated cut; (b) the USER EAR DECREE of 2026-09-09 ("the voice does not change mid sentence; we don't finish each others' sentences") retired the bounded-run cut entirely — atoms are never split across badges, the `cross_badge_fracture` waiver class is removed, and EVERY cross-badge fracture is a hard gate failure. Post-decree gate: 0 fractures of 180 persisted rows, 0 duplicate clusters, 0 overlapping-span rows, 0 unknown-within-cap, 0 zero-dur, 0 unmerged; RENDER-TEXT OK; churn 2.2 rows/min, 3 rows of ≤2 words; ear entries 15 PASS / 1 known-limitation (parked S2 ~12.0 s engine sub-run miss — a which-badge defect, not a fracture) / 0 FAILED. No amendment record is needed or present.
- [x] 3.2 `cargo test --lib` green (including the rewritten alignment tests) — DONE 2026-09-08: 628 passed / 0 failed / 19 ignored (release+vulkan, CARGO_TARGET_DIR=C:/mf).
- [x] 3.3 Live combined Speakers run on cde5c264 — DONE 2026-09-08/09, agent-driven twice (no UI click): first run 2026-09-08 25.2 min under the cut design (220 segments → 188 rows, 2 speakers, unmatched=[]); after the ear decree, second run 2026-09-08 25.5 min whole-atom (201 segments → 173 rows, 2 speakers, unmatched=[]). Gap-speech 5.2 anchor intact ("Oh, man" begins the Speaker 0 row at 14.78). "Where is Ricardo I don't know." persists WHOLE under one badge — which-badge correction owned by the parked engine follow-up. Driven via `run_speakers_reset_standalone` (`tests/live_speakers_run.rs`, ignored) — the app-button path hung once at pyannote (99% CPU, 2 h silent; parked).
- [x] 3.4 Post-live re-pin — DONE 2026-09-09: fixture re-pinned from the post-run DB (173 rows, `row_sha256 2952af…`), pre-live snapshot preserved at `cde5c264_transcripts.pre-live.json`; gate GREEN (exit 0) on the new snapshot; snapshot-script row-count guard relaxed from exact-240 to ≥100 (a live run legitimately moves the count via duplicate absorption + consolidation).
- [x] 3.5 Archive gate: `sentence-aware-turn-assembly` archived first (2026-09-13; its "Persisted speaker turns are sentence-readable" requirement now in the main spec); D1's reattribution clause reconciles with its no-merge rule (the "Sentences are not split across speaker badges" requirement states reattribution-is-not-a-merge verbatim); the canonical queue-phase postscript is superseded (annotation inline in the token-timestamps amendment bullet) and the headline requirement renamed to drop the stale "queue phase" phrasing; then archived
- [x] 3.6 Reconciliation with `align-from-immutable-source` (recorded 2026-09-09, while both changes are live): that change's MODIFIED requirement for "Token-level timestamps align transcript text with diarization speaker boundaries" SUPERSEDES this change's pending delta wording wherever they conflict — including this delta's "read token timestamps from the `transcripts` table" (becomes `transcript_sources`), the N=1 in-place relabel clause, the NULL-token shape-freeze idempotency clause, and the "id-set identical (strict equality)" scenario (all replaced by regeneration persist / re-derivation idempotency). BINDING ORDER: no-split archives FIRST; align SHALL NOT archive before it. At no-split's archive, carry forward (do not lose) this change's still-valid guarantees — they are restated in align's delta (word-content preservation, CJK divide, host-param ceiling, transaction atomicity). Also annotate as moot under align's regeneration persist: D4's "delete absorbed shells in the persist transaction" clause and the alignment.rs:546-548 stale comment (regeneration has no shells and no empty-group rule)

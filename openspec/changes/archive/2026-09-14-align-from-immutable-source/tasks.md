## 1. Storage: source table + eager backfill

- [x] 1.1 Migration: create `transcript_sources` mirroring the transcription-era columns of `transcripts` (id TEXT PRIMARY KEY, meeting_id TEXT NOT NULL, transcript TEXT NOT NULL, timestamp TEXT NOT NULL, summary, action_items, key_points, speaker, audio_start_time REAL, audio_end_time REAL, duration REAL, token_timestamps TEXT) plus `source_origin TEXT NOT NULL DEFAULT 'stt'`; backfill from `transcripts` for every existing meeting (id preserved, `source_origin = 'backfilled'` on all migrated rows); log per meeting the count of backfilled rows with `token_timestamps IS NULL` (the replaced-row signature) — design D1/D2. Migration is single-transaction (sqlx default; no `-- no-transaction` marker) — DONE 2026-09-09: `migrations/20260909000000_add_transcript_sources.sql`; the per-meeting NULL-token log runs in `manager.rs` (the migration is applied → `log_backfilled_null_token_rows` warns per meeting, detected via a before/after `list_applied_migrations` diff so it logs exactly once at upgrade)
- [x] 1.2 Writer audit (DONE in round-1 panel, re-verified 2026-09-09 at implementation): `INSERT INTO transcripts` across compiled (non-test) code hits exactly import.rs (import save), retranscription.rs (retranscription save), transcript.rs (`save_transcript`); every other hit is `#[cfg(test)]` or the rendering-lane split insert inside `persist_aligned_splits` (retired by 2.2). Audit result recorded here; inventory unchanged from the round-1 panel
- [x] 1.3 Shared dual-write helper in the transcript repository: insert into `transcripts` AND `transcript_sources` in one transaction; migrate all three writer sites onto it; retranscription's `DELETE FROM transcripts WHERE meeting_id = ?` gains the matching source delete; `delete_meeting` gains an explicit `DELETE FROM transcript_sources WHERE meeting_id = ?` beside the transcripts delete (do not rely on the FK cascade — house pattern is explicit deletes) — design D1/D3 — DONE: `TranscriptsRepository::insert_transcription_row`; all three writers call it inside their existing transactions
- [x] 1.4 Unit tests: backfill copies rows verbatim with `source_origin='backfilled'`; meeting deletion through the production repository path leaves zero `transcript_sources` rows; the dual-write helper leaves zero rows in either table on a rolled-back transaction; migration failure mid-backfill leaves the DB on the pre-migration schema (transaction rollback) — DONE: `tests/transcript_sources_migration.rs` (4 tests, green; the mid-backfill failure is injected via a RAISE(ABORT) trigger inside the migration transaction, DDL included, and the rollback is asserted to remove even the new table)

## 2. Pipeline: regeneration persist

- [x] 2.1 Retarget BOTH read sites to `transcript_sources` unconditionally (no fallback branch): `fetch_transcripts_for_alignment` and `fetch_transcript_timestamps` (engine priors for `derive_turns`); add the D8 drift warning (rendering non-empty ∧ source empty → warn with meeting id); tests: engine priors come from the source table when source and rendering spans differ, and the drift warning fires with the run no-opping on source-empty/rendering-non-empty — design D5/D8 — DONE: drift check is `detect_source_rendering_drift` (returns bool as a test hook), invoked inside the aligner fetch; tests `engine_priors_come_from_the_source_table` / `alignment_input_comes_from_source_and_drift_warns` in commands.rs
- [x] 2.2 Implement the regeneration persist (design D4) replacing `persist_aligned_groups`/`persist_aligned_splits`/`delete_absorbed_source_row`: one transaction — load surviving manual rows → suppress segments whose midpoint falls inside a surviving manual span → delete all non-manual rendering rows → insert fresh-UUID rows copying template columns from the SOURCE row → abort if aligned output is empty while source is non-empty. Retire the absorbed-row sweep and the in-place relabel path — DONE: `SpeakerRepository::persist_regenerated_rendering`; midpoint predicate is half-open [start, end) matching the aligner's containment; manual survival keys on `speaker_source` alone (a NULL-timing manual row survives but claims no span); commands.rs Step 8 rewired; stale empty-group comments fixed in commands.rs and alignment.rs (`resolve_duplicate_clusters` doc)
- [x] 2.3 Row-identity regression tests (the round-1 blocking hole): in-memory DB seeded with identical rows in both tables — (a) split run followed by an identical re-run: row count and (text, span, badge) multiset identical (no absorbed-sweep shrink, no silent `Ok(0)` discard); (b) split run followed by a whole-row single-segment re-run: the split re-expands to one full-text row; (c) re-run after consolidation-shaped prior output: rebuilt, not swept; (d) word-content preservation: the rendering's word multiset equals the source's minus duplicate-absorbed copies and punctuation-only ranges, with manual-claimed spans exempt (the generalized all-words guarantee) — DONE: (a) `regeneration_second_identical_run_is_stable`, (b) `regeneration_reexpands_split_when_engine_changes`, (c) `regeneration_rebuilds_consolidation_shaped_rendering`, (d) `regeneration_word_multiset_preservation` (fixture models duplicate_pair's real predicate: disjoint spans, ≤2 s gap, different badges)
- [x] 2.4 Manual-guard tests: manual rendering row survives regeneration untouched (id, text, label, `previous_label` intact); segments with midpoints inside the manual span suppressed (no duplicate text); a straddling segment follows the midpoint predicate (suppressed or kept whole — pin the exact behavior in the test); `rederive_manual` regenerates everything; regenerated rows carry `previous_label = NULL`; degenerate empty alignment aborts with prior rendering rows intact — DONE: `manual_row_survives_regeneration_and_claims_by_midpoint` (pins: midpoint inside → suppressed; straddler with midpoint OUTSIDE → kept whole), `manual_row_rederives_only_when_explicitly_allowed`, `regenerated_rows_carry_previous_label_null`, `degenerate_empty_alignment_aborts_rendering_intact`
- [x] 2.5 Re-run idempotence test at the pure stages (design D7): fixed synthetic diarization segments (no live clustering), same source aligned+persisted twice → byte-identical auto rendering except fresh ids; run against BOTH a full-punctuation synthetic source and the cde5c264 pre-live snapshot as a frozen-degraded source (the 237→240→188→173 drift pattern must be structurally impossible). The snapshot fixture is UNTRACKED (cleanup 5.3 gitignores it): guard with the house exists()-skip + printed-notice pattern so fresh clones and CI stay green; seeding note: the fixture stores milliseconds, `transcripts`/`transcript_sources` store seconds — convert on seed. Record the rejoin-unit observations (design Risks): unit count/sizes and how many units degrade to proportional spans under pristine-row input — DONE: `pure_stage_double_run_idempotent_full_punctuation_source`, `pure_stage_double_run_idempotent_pre_live_snapshot` (exists()-skip; ms→seconds on seed; both green). OBSERVATIONS (2026-09-09, `rejoin_unit_observations_on_pre_live_snapshot`): the pre-live snapshot's 240 input rows rejoin into 40 units — words/unit mean 344, max 1922 (≈10 min of audio in ONE unit) — and 100% of units degrade to proportional spans (every backfilled row carries NULL `token_timestamps`, so no unit can pass the token clamp). The design-risk prediction is confirmed with measurement; a follow-up engine change (if wanted) starts from these numbers
- [x] 2.6 Punctuation-invariant test (design D6): fixture with multi-sentence source rows through align → merge → regeneration keeps every alphanumeric sentence's terminator present in ≥1 persisted rendering row; duplicate-cluster absorbed members, punctuation-only ranges, and manual-claimed spans exempt; same assertion added to `merge_same_label_fragments` unit tests — DONE: `punctuation_invariant_survives_pipeline` (all three exemptions exercised end-to-end; asserts "Where is Ricardo?" survives), `merge_same_label_fragments_preserves_sentence_terminators` in commands.rs
- [x] 2.7 `cargo test --lib` green, including rewritten legacy persist tests (the old replace-by-id and in-place tests are superseded by 2.2-2.4) — DONE 2026-09-09: 648 passed / 0 failed / 19 ignored. NOTE: three PRE-EXISTING broken external test targets (`enhance_speakers_live`, `pyannote_ort_probe`, `speaker_diarization_integration` — broken at HEAD by the 5-arg `run_diarization_for_meeting` + `pub(crate)` policy they cannot name) were repaired via the new pub wrapper `run_diarization_for_meeting_auto`; `cargo check --tests` is now error-free

## 3. Verification and coordination

- [x] 3.1 Spec reconciliation (BINDING ARCHIVE ORDER: this change SHALL NOT archive before `no-split-sentences`; after both archive, THIS change's MODIFIED requirement text is the sole surviving text for the "Token-level timestamps align transcript text with diarization speaker boundaries" requirement): record the supersession NOW, while both changes are live, in no-split's tasks (its round-1/2 wording "read token timestamps from the `transcripts` table", its N=1 in-place clause, its NULL-token shape-freeze idempotency clause, and its "id-set identical (strict equality)" scenario are ALL superseded by this change's MODIFIED requirement); annotate as moot under regeneration: no-split D4's "delete absorbed shells in the persist transaction" clause AND the stale comment at alignment.rs:546-548 ("the persist pass deletes their source rows — empty-group rule" — regeneration has no shells and no empty-group rule) — DONE: no-split tasks 3.6 carries the full reconciliation + binding order (written while both changes are live); the alignment.rs comment has been rewritten in code to the regeneration wording
- [x] 3.2 No-split gate continuity: amend the snapshot tooling and gate replay input to `transcript_sources` (the gate's replay input becomes the pipeline's actual input post-split); SEQUENCING: the live runs in 3.3 wait until `no-split-sentences` archives (its SHA-pinned freeze owns cde5c264 until then), then re-pin per the no-split re-pin regime — DONE (tooling): `snapshot_fixture.py` reads `transcript_sources` (falls back to `transcripts` only on a pre-migration DB and prints which table it read); `ear_truth_gate.rs` render-replay cross-check reads `transcript_sources` with a loud "run the app once so the migration applies, then re-pin" failure if the table is absent. SEQUENCING recorded: the next snapshot/re-pin happens only after no-split archives
- [ ] 3.3 Live check on cde5c264 (after 3.2's sequencing): after migration the meeting's source equals its (degraded) rendering; two consecutive Speakers runs produce a structurally stable rendering (row count + text multiset; badge jitter investigated, not gated); then retranscribe + re-run Speakers to verify the healing path end-to-end (source regains full punctuation; "Where is Ricardo?" reappears splittable; source `token_timestamps` non-NULL) — CORE DONE 2026-09-13, headless (no UI; no-split archived 2026-09-13, satisfying 3.2's sequencing; DB backed up first as `meeting_minutes.sqlite.bak-pre-sources-backfill`). Via `tests/live_alignment_source_check.rs` (new ignored harness): (1) canonical migrator applied → BACKFILL REPORT 4 meetings / 651 rows, ALL NULL-token (replaced-row signature; cde5c264 frozen at 182 rows); parity proven THREE ways — source rows == rendering rows (id-joined field equality), count 182 == 182, and the source's canonical hash `af1e2465…` == no-split's archived pinned `row_sha256` (the archive pin and the backfill agree row-for-row, so the ear-gate cross-check stays green through real runs). (2) Two consecutive full Speakers runs (~6.6 min each, release): source hash BYTE-IDENTICAL across both runs (the headline — no run consumes its own output); rendering structurally stable — 184 == 184 rows, text multiset identical, badge distribution identical (Cynthia Wu 94 / Speaker 0 90 — zero jitter); the historical 237→240→188→173→182 drift is dead (182-converged-input → 184 → 184). "Where is Ricardo?" verified present with its '?' in the frozen source. REMAINING (retranscribe leg): the healing end-to-end run is owned by `whisper-hallucination-cleanup` 5.2 (window repair → re-pin → Speakers run re-derives from the healed source), whose sequencing required exactly this change to land — it is now unblocked; the healing STORAGE path (clean-slate dual delete + dual-write) is unit-proven in 1.3/1.4. Do NOT retranscribe ad hoc before 5.2: it would invalidate cleanup's expected-flags baseline mid-lane
- [ ] 3.4 Cleanup handoff (mechanized acceptance lives in `whisper-hallucination-cleanup` 4.2/5.2 — its REPAIR_WRITE integration test asserts the repaired row exists in `transcript_sources` AND a subsequent align returns the repaired text): confirm before that change's repair run that the handoff test exists and passes — NOT YET EXECUTABLE: cleanup 4.2/5.2 are implemented only to the PRE-revision spec (their status note: the source-first revision — source audit, fresh token JSON in `transcript_sources`, `source_origin = 'stt'` stamp, N-rows-per-window — is the remaining work, sequenced behind this change). The storage surface they target is now LIVE-VERIFIED (production DB migrated, parity + byte-stability proven in 3.3); nothing further for this change to do until their refinement lands

## Coordination status

- `no-split-sentences` ARCHIVED 2026-09-13 (`archive/2026-09-13-no-split-sentences`) — the binding order (no-split first) is satisfied; this change MAY now proceed to archive after its own tasks complete. Its still-valid guarantees (word-content preservation, CJK divide, host-param ceiling, transaction atomicity) are carried in the canonical spec via no-split's synced delta, as required by the reconciliation record.

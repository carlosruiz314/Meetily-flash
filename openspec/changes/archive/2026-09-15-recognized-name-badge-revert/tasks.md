# Tasks: recognized-name-badge-revert

## 1. Backend revert extension

- [x] 1.1 `SpeakerRepository::revert_speaker_label` recognized-name path (design D1/D2): after the unchanged manual-path UPDATE, recover the cluster label(s) via `speaker_embeddings` joined on `speakers.name`, relabel rows carrying the bare name (`previous_label IS NULL`) to the lowest recovered label with `speaker_source = NULL`; return the sum of both paths — DONE 2026-09-15: `database/repositories/speaker.rs`
- [x] 1.2 Unlink extension (design D3): gate on the combined count; empty-`originals` shape omits the `cluster_label IN ()` clause (invalid SQL when interpolated empty) and unlinks by the named-speaker id alone — DONE 2026-09-15
- [x] 1.3 Tests: `recognized_name_revert_restores_cluster_label_and_unlinks` (relabel to the recovered label, unrelated cluster untouched, `speaker_id` unlinked); `recognized_name_over_split_clusters_collapse_to_lowest_label` (design D2); test 4.3's minimal pool gains `speakers`/`speaker_embeddings` (revert now consults them even on the not-revertible path) — DONE 2026-09-15
- [x] 1.4 Speaker test surface green — DONE 2026-09-15: `cargo test --lib speaker` 325 passed / 0 failed / 12 ignored (post-review re-run; pre-review run was 324)
- [x] 1.5 Frontend: intentionally unchanged (proposal "What changes") — DONE 2026-09-15; an interim gating attempt (revertible-labels listing command + set-based icon gate) was fully reverted to HEAD before this design landed
- [x] 1.6 Review round 1 fixes (code-reviewer subagent, verdict "With fixes") — DONE 2026-09-15: IMPORTANT — added the mixed manual+recognized scenario test the spec delta promises (`revert_handles_mixed_manual_and_recognized_rows_under_one_name`, pins the `previous_label IS NULL` path-disjointness); MINOR 2 — recovery ORDER BY is numeric (`CAST(substr(cluster_label, 9) AS INTEGER)`, lexical would pick "Speaker 10" over "Speaker 2"; 4.4b fixture extended to pin it); MINOR 3 — the whole revert runs in one transaction (a partial relabel-without-unlink could never self-heal on retry); MINOR 5 — the NULL-label-override sweep documented in the code comment and design D4. NOT ADOPTED: MINOR 4 (scalar subquery with duplicate speaker names — UI-unreachable, identical shape pre-exists in `relink_meeting_embeddings`) and MINOR 6 (count inflation in the previous_label == speaker_label ping-pong corner — final label is the more correct original, count discarded by the UI). Reviewer's unverified claims (324-run, live-DB simulation) were implementer-verified and re-verified post-fix (325 run above)

## 2. Verification

- [x] 2.1 Live-DB read-only simulation on cde5c264: "Cynthia Wu" → recovered label "Speaker 1" (1 embedding row), 108 rows would relabel — DONE 2026-09-15
- [x] 2.2 Frontend suites green on the untouched tree — DONE 2026-09-15: vitest 24 files / 279 tests pass (one unrelated first-run flake, clean rerun); `tsc --noEmit` shows the same 8 pre-existing errors as HEAD (no frontend files touched)

## 3. Archive

- [x] 3.1 Archive (DONE 2026-09-15): spec delta applied to the canonical requirement "Retroactive speaker labeling via inline badges with per-speaker revert"; folder moved to `changes/archive/2026-09-15-recognized-name-badge-revert`. Blocker re-check before archiving: no live change claims the amended requirement (grep over `changes/*/specs/` — the only hit was this folder); the never-labeled-row limitation scenario in the per-segment-override requirement remains accurate under the new recovery path (its GIVEN — diarization skipped — means no embeddings exist to recover from). NOTE: the superseded canonical scenario "Revert disabled for auto-generated labels and legacy manual labels" claimed the undo icon is hidden when `previous_label IS NULL` — the code never implemented that clause (the name heuristic cannot see previous_label); the adopted text replaces it with the truth (icon offered on all non-default names, recognized names now revertible, never-labeled-without-embedding stays a documented limitation via test 4.3 and design D4)
